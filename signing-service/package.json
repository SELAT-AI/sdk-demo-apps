{
  "name": "selat-signing-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hosted remote signer for SELAT Circle Agent Wallets. Runs the Circle CLI on a long-lived host and exposes POST /sign for serverless apps that use createHttpRemoteSigner.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@circle-fin/cli": "^0.0.6",
    "@selat-ai/router-client": "^0.1.2"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.21.0",
    "typescript": "^5.8.0"
  }
}
